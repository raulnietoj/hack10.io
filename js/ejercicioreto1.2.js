 
 //2. Crea una función que retorne la potencia de un número dado, esta función
//deberá recibir la potencia y el número a potenciar
const potencia = (b,e) => console.log(`La base ${b} elevado a ${e} es ${Math.pow(b,e).toFixed(1)}`); 
potencia(5,3);
